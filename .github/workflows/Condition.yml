name: Print Job

on:
  workflow_dispatch:   # allows manual trigger
  schedule:
    # Example: run every hour at 00 minute
    - cron: "0 * * * *"

jobs:
  print-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Triggered at $(date)"
